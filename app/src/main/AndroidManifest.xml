<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.absencemonitoring">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.SportlistDetailsActivity"></activity>
        <activity android:name=".activities.SportmanDashboardActivity">
        </activity>
        <activity android:name=".activities.EmployeeDashboardActivity" />
        <activity android:name=".activities.GuardDashboardActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.EmployeeTransportationReserveActivity" />
        <activity android:name=".activities.EmployeeSportReserveActivity" />
        <activity android:name=".activities.MasterDashboardActivity" />
        <activity android:name=".activities.MasterFurloughActivity" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activities.FurloughDetailsActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD6Xu0ql5wE2OEyjzcNyAtRr28ZqyI9F4s" />

        <activity android:name=".activities.EmployeeFurloughActivity" />
    </application>

</manifest>